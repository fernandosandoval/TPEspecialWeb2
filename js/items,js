$(document).ready(function(){

   $.ajax({
       url: "http://localhost/2017/TPE/items",
       success: function(result){
        //  for (var i = 0; i < 1000000000; i++) {
        //  }
         $("#items").html(result);
       }
   });

 });
